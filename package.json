{
  "name": "@latchprotocol/dex-token-list",
  "version": "0.0.0",
  "private": true,
  "description": "Latch token lists (Uniswap Token Lists standard), one per chain. Generated in the Latch monorepo; this repository is the published copy.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node scripts/check.mjs --dir . --git-ref origin/main",
    "check:chain": "node scripts/check.mjs --dir . --git-ref origin/main --chain"
  },
  "devDependencies": {
    "@uniswap/token-lists": "^1.0.0-beta.35",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "viem": "^2.21.0"
  }
}
